# cargo-audit configuration
# https://rustsec.org/

[advisories]
ignore = [
    # rustls-pemfile unmaintained - comes from reqwest 0.11 via rustls
    # Will be resolved when we upgrade to reqwest 0.12+ (requires hyper 1.x migration)
    "RUSTSEC-2025-0134",
]
unmaintained = "warn"
