# Captcha Images

This directory would contain dynamically generated server-side captcha images.

Images are generated on-demand and not stored permanently.

No pre-generated images are included in the repository.
